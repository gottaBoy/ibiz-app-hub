export const en = {
  globalPlugin: {
    loadSuccess: 'Global plugin loaded successfully',
    noExpandTag: 'The expansion identifier is not configured',
    noCollapseTag: 'The fold identifier is not configured',
  },
} as const;

export default en;
