export const en = {
  deAction: {
    dynamicCodeListItem: 'Dynamic code list item {index}',
    createMissingData: 'The create action requires data.',
    updateMissingData: 'The update action requires data.',
  },
} as const;

export default en;
